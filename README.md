# Tandwielwerkplaats

Codex-versie met verbeterde ontdekmodus.

## Bestanden uploaden

Upload `index.html`, `style.css`, `game.js` en de map `assets` naar GitHub.

## Starten

Open `index.html` in Chrome, Edge of op een Prowise-scherm.
